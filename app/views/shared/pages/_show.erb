<% # locals: (fields:, object:, show_actions_args: {}) %>
<%# KEEP%>

<%= render(QuickActionsCardComponent.new) do %>
  <% if content_for?(:custom_show_actions) %>
    <% yield :custom_show_actions %>
  <% else %>
    <%= render '/shared/show_actions', object: object, **show_actions_args %>
  <% end %>
<% end %>

<%= render CardComponent.new(flush: true) do %>
  <%= render 'shared/basic_fields', fields: fields %>
<% end %>
