<% search_fields = {
  date_cont:            { slug: 'defaults.date' },
  users_full_name_cont: { slug: 'defaults.user' },
} %>

<%= render 'shared/pages/index', resource_class: MaintenanceSession, resources: @maintenance_sessions, content: render('index_results'), search_fields: search_fields %>
