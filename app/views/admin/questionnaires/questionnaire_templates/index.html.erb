<% quick_actions = get_link Admin::Questionnaires::Questionnaire, :index, link_text: 'Back to Questionnaires'%>

<% headers = [:name] %>

<% field_sets = @questionnaire_templates.map { |questionnaire_template| { fields: [questionnaire_template] } } %>

<% search_fields = {
    name_cont: { slug: 'defaults.name' }
} %>

<%= render 'shared/pages/index', resource_class: Admin::Questionnaires::QuestionnaireTemplate, resources: @questionnaire_templates, headers: headers, field_sets: field_sets, search_fields: search_fields,
  quick_actions: quick_actions %>
