# == Schema Information
#
# Table name: maintenance_sessions
#
# *id*::         <tt>bigint, not null, primary key</tt>
# *date*::       <tt>date</tt>
# *created_at*:: <tt>datetime, not null</tt>
# *updated_at*:: <tt>datetime, not null</tt>
#--
# == Schema Information End
#++
one:
  date: '2023-10-09'

# For testing that deletion is allowed when there are no attendances
no_attendances:
  date: '2023-10-10'

# Has an explicit name, for testing the name feature and to_label fallback.
named:
  name: 'Autumn deep clean'
  date: '2023-10-11'
