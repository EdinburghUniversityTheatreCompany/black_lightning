<% headers = [:name, 'Amount of Users'] %>

<% field_sets = @roles.map { |role| { fields: [role, role.users.size] } } %>

<%= render 'shared/pages/index_results', resources: @roles, resource_class: Role, headers: headers, field_sets: field_sets %>
