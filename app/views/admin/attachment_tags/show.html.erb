<% fields = { 
  name: @attachment_tag.name,
  ordering: @attachment_tag.ordering,
  description: { type: 'markdown', markdown: @attachment_tag.description } 
} %>

<% content_for :extra_show_actions do %>
  <%= get_link_to_attachments_for_tag(@attachment_tag) %>
<% end %>

<%= render('shared/pages/show', object: @attachment_tag, fields: fields) %>
