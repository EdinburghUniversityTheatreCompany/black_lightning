<% quick_actions = [get_link(Admin::Staffing, :index, link_text: 'Back to Staffings')] %>

<% headers = [:name] %>

<% field_sets = @staffing_templates.map { |staffing_template| {fields: [staffing_template] } } %>

<% search_fields = {
    name_cont: { slug: 'defaults.name' }
} %>

<%= render 'shared/pages/index', resource_class: Admin::StaffingTemplate, resources: @staffing_templates, headers: headers, field_sets: field_sets, search_fields: search_fields,
  quick_actions: quick_actions %>
