<%# Based on the very similar _team_member_fields.erb file %>
<% # BOOTSTRAP NICETOHAVE: Margins between the fields. Also for other form-inlines %>

<%= render layout: '/shared/form/fields_for_template', locals: { 
  f: f,
  div_class_additions: 'job form-inline'
} do %>
    <%= f.hidden_field :id %>

    <%= f.input(:name, placeholder: 'Job', label: false) %>

    <%= f.input_field :user, collection: User.all %>

    <%= link_to_remove f %>
<% end %>
