<% quick_actions = get_link Admin::Proposals::Call, :index, link_text: 'Back to Proposal Calls' %>

<% headers = [:name] %>

<% field_sets = @call_question_templates.map { |template| { fields: [template] } } %>

<% search_fields = {
    name_cont: { slug: 'defaults.name' }
} %>

<%= render 'shared/pages/index', resource_class: Admin::Proposals::CallQuestionTemplate, resources: @call_question_templates, headers: headers, field_sets: field_sets, search_fields: search_fields,
  quick_actions: quick_actions %>
