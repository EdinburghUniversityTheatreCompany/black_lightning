<% # BOOTSTRAP: Alex, can you check if this looks like you want it to look or if bits that are currently in the _table partial need to be moved to the generic index partial? %>

<% # TODO: Generic date thing which works for all types of attachments... TEST IT BEFORE CONTINUING %>
<% #= render 'shared/date_range_search_form_field', f: f %>

<% search_fields = {
    name_cont: { slug: 'defaults.name' },
    file_blob_filename_cont: { slug: 'defaults.file_name' },
    attachment_tags_id_eq: { slug: 'defaults.attachment_tags', type: :select, collection: AttachmentTag.accessible_by(current_ability) }
} %>

<% content = render('admin/attachments/index_content', q: @q, attachments: @attachments) %>

<%= render 'shared/pages/index', resource_class: Attachment, resources: @attachments, content: content, search_fields: search_fields, 
  show_new_button: false %>
