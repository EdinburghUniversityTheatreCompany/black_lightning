<% search_fields = {
    subject_cont:     { slug: 'defaults.subject' },
    description_cont: { slug: 'defaults.description'},
    comments_cont:    { slug: 'defaults.comments'},
} %>

<%= render 'shared/pages/index', resource_class: Complaint, resources: @complaints, content: render('index_results'), search_fields: search_fields, show_new_button: false %>
