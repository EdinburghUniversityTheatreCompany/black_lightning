<% search_fields = {
    card_number_cont: { slug: 'defaults.card_number' },
    user_full_name_cont: { slug: 'defaults.name' }
} %>

<%= render 'shared/pages/index', resource_class: MembershipCard, resources: @cards, content: render('index_results'), search_fields: search_fields %>
