<% fields = {
  name: @maintenance_session.name,
  date: @maintenance_session.date,
  users: { type: 'content', header: "Attendees", content: render(UsersTableComponent.new(users: @users, url: admin_maintenance_session_url(@maintenance_session), q: @q, credit_counts: @credit_counts)) }
} %>

<%= render('shared/pages/show', object: @maintenance_session, fields: fields) %>
