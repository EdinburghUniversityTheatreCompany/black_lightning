<% search_fields = {
    title_cont:             { slug: 'defaults.title' },
    description_cont:       { slug: 'defaults.description' },
    creator_full_name_cont: { slug: 'defaults.creator' }
} %>

<%= render 'shared/pages/index', resource_class: Opportunity, resources: @opportunities, content: render('index_results'), search_fields: search_fields %>
