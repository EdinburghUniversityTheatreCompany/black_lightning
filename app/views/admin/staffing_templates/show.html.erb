<% fields = {} %>

<% @staffing_template.staffing_jobs.each do |job| %>
  <% fields[job.id] = { type: 'content', content: job.name } %>
<% end %>

<%= render('shared/pages/show', object: @staffing_template, fields: fields) %>
