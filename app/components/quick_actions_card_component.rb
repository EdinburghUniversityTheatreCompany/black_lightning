# The strip of action buttons above a show page or an index. Takes its buttons
# as the block content, so a caller passes get_link calls straight through.
class QuickActionsCardComponent < ViewComponent::Base
end
