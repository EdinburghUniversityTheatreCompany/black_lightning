<div class="grid gap-3 grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
  <% @categories.each do |category| %>
    <div>
      <div class="card h-full">
        <% img = capture do %><%= render 'shared/image', image: category.fetch_image, variant: square_thumb_variant(300), image_options: { class: 'card-img-top h-auto' } %><% end %>
        <%= get_link category, :show, link_text: img %>

        <div class="card-body">
          <p class="card-text"><%= get_link category, :show %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div class="mb-3"></div>
