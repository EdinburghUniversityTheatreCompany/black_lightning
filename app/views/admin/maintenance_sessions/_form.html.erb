<%# locals: () %>

<%= simple_horizontal_form_for([:admin, @maintenance_session]) do |f| %>
  <%= render('shared/pages/form', f: f) do %>
    <%= f.input :name %>
    <%= f.input :date %>

    <%= render('shared/form/sections/nested_fields', f: f, association_name: :maintenance_credits, partial_name: 'maintenance_credit_fields', collection: @maintenance_session.attendees_for_form) %>
  <% end %>
<% end %>
