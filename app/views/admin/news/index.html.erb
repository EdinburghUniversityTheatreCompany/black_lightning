<% search_fields = {
    title_cont: { slug: 'defaults.title' },
    body_cont:  { slug: 'defaults.body' }
} %>

<%= render 'shared/pages/index', resource_class: News, resources: @news, content: render('index_results'), search_fields: search_fields %>
