<%# Based on the very similar _team_member_fields.erb file %>

<%= render '/shared/form/inline_fields_for_template', 
  f: f,
  div_class_additions: 'job',
  fields: [
    { field: f.hidden_field(:id) + f.input(:name, placeholder: 'Job', wrapper: :inline_form), col_modifier: 'auto' },
    { field: render('shared/form/user_field', f: f, wrapper: :inline_form) },
    { field: link_to_remove(f), col_modifier: 'auto' },
  ] %>
