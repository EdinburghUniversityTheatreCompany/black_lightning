Hi <%= @person.name.presence || "there" %>,

Good news: EUSA has paid <%= @expenses.size == 1 ? "your expense" : "your expenses" %>.
The money should reach your account shortly, if it hasn't already.

<% @expenses.each do |expense| -%>
- <%= expense.description.presence || "Expense ##{expense.auto_number}" %>: <%= number_to_currency(expense.amount || 0, unit: "£") %>
<% end -%>

Thanks for your patience, and thanks for everything you do for Bedlam Fringe.

The Bedlam Fringe finance team
