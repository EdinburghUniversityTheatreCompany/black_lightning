<%# locals: (expense:, upload_errors: [], readonly: false) %>
<%# Replaced wholesale by turbo streams from receipts#create/#destroy; the
    fancybox and receipt-viewer controllers reconnect with the new DOM on each
    replacement. readonly hides the remove control for the view-only show page.

    Display, thumbnails and the in-page pane all come from the shared
    shared/_receipt_viewer partial, the same one the Review queue and the finance
    expense pages use; this wrapper only adds the producer's upload errors and
    the per-thumbnail remove button. %>
<% remove_url = ->(receipt) {
     admin_reimbursements_expense_receipt_path(expense.record_id, receipt.attachment_id)
   } unless readonly %>
<% draft_note =expense.draft? ? " yet. A draft can wait, but the claim needs one before you submit it." : "." %>
<div id="receipts-gallery">
  <% upload_errors.each do |message| %>
    <p class="mb-2 text-sm text-danger"><%= message %></p>
  <% end %>

  <%= render "admin/reimbursements/shared/receipt_viewer",
             receipts: expense.receipts,
             key: expense.record_id,
             label: "expense ##{expense.auto_number}",
             empty_message: "No receipts attached#{draft_note}",
             remove_url: remove_url %>
</div>
