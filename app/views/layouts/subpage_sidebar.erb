<% # Inspiration from https://getbootstrap.com/docs/5.0/examples/sidebars/ %>
<% # Currently unused :( %>

<% content_for :content do %>
  <div class="flex gap-4">
    <div class="w-1/4">
      <%= render('layouts/sidebar/sidebar', title: @sidebar_title, subpages: @subpages) %>
    </div>
    <div class="w-3/4">
      <div class="card">
        <div class="card-body">
          <%= yield %>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%= render template: "layouts/#{current_environment(request.fullpath)}" %>
