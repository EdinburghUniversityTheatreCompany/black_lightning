<% search_fields = {
    name_cont:        { slug: 'defaults.name' }
} %>

<%= render 'shared/pages/index', resource_class: Group, resources: @groups, content: render('index_results'), search_fields: search_fields %>
