<% fields = { 
  send_date: get_send_date(@mass_mail),
  sent_by: get_sender_name(@mass_mail),
  body: { type: 'markdown', markdown: @mass_mail.body }
} %>

<%= render('shared/pages/show', object: @mass_mail, fields: fields) %>
