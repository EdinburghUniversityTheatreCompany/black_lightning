<% search_fields = {
    item_cont:          { slug: 'defaults.item' },
    description_cont:   { slug: 'defaults.description' },
    severity_eq:        { slug: 'defaults.severity' },
    status_eq:          { slug: 'defaults.status' }
} %>

<%= render 'shared/pages/index', resource_class: FaultReport, resources: @fault_reports, content: render('index_results'), search_fields: search_fields %>
