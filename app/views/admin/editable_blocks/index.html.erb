<% search_fields = {
    name_cont:    { slug: 'defaults.name' },
    content_cont: { slug: 'defaults.content' },
    group_cont:   { slug: 'defaults.group' },
    url_cont:     { slug: 'defaults.url' }
} %>

<%= render 'shared/pages/index', resource_class: Admin::EditableBlock, resources: @editable_blocks, content: render('index_results'), search_fields: search_fields %>
