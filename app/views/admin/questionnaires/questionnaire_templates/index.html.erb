<h1>Questionnaire Templates</h1>

<table class="table">
  <tr>
    <th>Name</th>

    <% if can? :edit, Admin::Questionnaires::QuestionnaireTemplate %>
    <th></th>
    <% end %>

    <% if can? :delete, Admin::Questionnaires::QuestionnaireTemplate %>
    <th></th>
    <% end %>
  </tr>

<% @templates.each do |template| %>
  <tr>
    <td><%= template.name %></td>

    <% if can? :edit, template %>
    <td><%= link_to 'Edit', edit_admin_questionnaires_questionnaire_template_path(template) %></td>
    <% end %>

    <% if can? :delete, template %>
    <td><%= link_to 'Destroy', template, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <% end %>
  </tr>
<% end %>
</table>

<br />

<% if can? :create, Admin::Questionnaires::QuestionnaireTemplate %>
<%= link_to 'New Template', new_admin_questionnaires_questionnaire_template_path, :class => "btn" %>
<% end %>
