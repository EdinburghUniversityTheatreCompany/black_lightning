<% fields = { 
  name: @picture_tag.name,
  description: { type: 'markdown', markdown: @picture_tag.description } 
} %>

<% content_for :extra_show_actions do %>
  <%= get_link_to_pictures_for_tag(@picture_tag)%>
<% end %>

<%= render('shared/pages/show', object: @picture_tag, fields: fields) %>
