<% fields = { 
  item: @fault_report.item,
  severity: @fault_report.severity.humanize,
  status: @fault_report.status.humanize,
  reported_by: @fault_report.reported_by_name,
  fixed_by: @fault_report.fixed_by_name,
  description: { type: 'markdown', markdown: @fault_report.description }
} %>

<%= render('shared/pages/show', object: @fault_report, fields: fields) %>
