<% search_fields = {
    name_cont:  { slug: 'defaults.name' },
    about_cont: { slug: 'defaults.about'}
} %>

<%= render 'shared/pages/index', resource_class: MarketingCreatives::Profile, resources: @profiles, content: render('index_results'), search_fields: search_fields %>
