# == Schema Information
#
# Table name: roles
#
# *id*::            <tt>integer, not null, primary key</tt>
# *name*::          <tt>string(255)</tt>
# *permissions*::
# *resource_id*::   <tt>integer</tt>
# *resource_type*:: <tt>string(255)</tt>
# *created_at*::    <tt>datetime, not null</tt>
# *updated_at*::    <tt>datetime, not null</tt>
#--
# == Schema Information End
#++

# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html
#
# Member only has access backend permission and sign_up_for staffing permissions. Try to not give them additional permissions using fixtures.
# This makes it possible to use member to test the 'default' settings.
member:
   id: 1
   name: "member"

# Committee has extra permissions and can thus be used for testing.
# You can also give committee extra permissions using the fixtures for testing.
committee:
  id: 2
  name: "committee"

admin:
  id: 3
  name: "admin"
  