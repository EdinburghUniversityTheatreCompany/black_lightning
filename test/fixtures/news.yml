# == Schema Information
#
# Table name: news
#
# *id*::                 <tt>integer, not null, primary key</tt>
# *title*::              <tt>string(255)</tt>
# *body*::               <tt>text</tt>
# *slug*::               <tt>string(255)</tt>
# *publish_date*::       <tt>datetime</tt>
# *show_public*::        <tt>boolean</tt>
# *created_at*::         <tt>datetime, not null</tt>
# *updated_at*::         <tt>datetime, not null</tt>
# *image_file_name*::    <tt>string(255)</tt>
# *image_content_type*:: <tt>string(255)</tt>
# *image_file_size*::    <tt>integer</tt>
# *image_updated_at*::   <tt>datetime</tt>
# *author_id*::          <tt>integer</tt>
#--
# == Schema Information End
#++

# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

one:
  title: "In the begining"
  slug: "begin"
  publish_date: <%= (3).days.ago.to_s(:db) %>
  show_public: true
  body: "In the begining there was news.  \n And the news was rendered with *markdown*. And the Lord saw that it was good. \n \n He also noted that if you create two new lines, a new paragraph begins. \n \n Finally, he saw that only the first paragraph after 140 characters was displayed in the preview."
  author_id: 1

two:
  title: "Then"
  slug: "then"
  publish_date: <%= (2).days.ago.to_s(:db) %>
  show_public: false
  body: "Then the Lord saw that he could create news that was only displayed to members. He did test the system, and found that it was good."
  author_id: 1

three:
  title: "Finally"
  slug: "finally"
  publish_date: <%= (1).days.ago.to_s(:db) %>
  show_public: true
  body: "This is some more news. That is all."
  author_id: 1
