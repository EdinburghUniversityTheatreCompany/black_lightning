<%# locals: (current_page:, url:, remote:, per_page:, total_pages:) %>

<li class="page-item <%= "disabled" if current_page.last? %>">
  <%= link_to raw(t 'views.pagination.last'), url, remote: remote, class: 'page-link' %>
</li>
