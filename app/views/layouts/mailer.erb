<% # This might need to be split in a text and html version, but since header and footer also each have a html and text version, that might not be necessary. %>

<%= render partial: "shared/mail/header" %>

<%= yield %>

<%= render partial: "shared/mail/footer" %>
