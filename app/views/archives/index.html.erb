<h2>Welcome to the Crypt</h2>

<p>
  Please select which archives you would like to view:
</p>

<ul>
  <li><%= link_to 'Shows', archives_shows_index_path(params[:start_month], params[:start_year], params[:end_month], params[:end_year]) %></li>
  
  <li>
  <% if current_user then %>
    <%= link_to 'Proposals', archives_proposals_index_path(params[:start_month], params[:start_year], params[:end_month], params[:end_year]) %>
  <% else %>
    <%= link_to 'Proposals', archives_proposals_index_path(params[:start_month], params[:start_year], params[:end_month], params[:end_year]), { :title => "You will need to log in to view proposal archives", :class => "muted" } %>
  <% end %>
  </li>
</ul>