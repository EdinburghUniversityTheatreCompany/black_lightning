<% fields = {} %>

<% @call_question_template.questions.each do |question| %>
  <% fields[question.id] = { type: 'content', content: render('/shared/question_template_show', question: question) } %>
<% end %>

<%= render('shared/pages/show', object: @call_question_template, fields: fields) %>
