<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @season.name %>
</p>

<p>
  <b>Description:</b>
  <%= render_markdown @season.description %>
</p>

<p>
  <b>Dates:</b>
  <%= I18n.l(@season.start_date, :format => :long) %> – <%= I18n.l(@season.end_date, :format => :longy) %>
</p>


<% edit_text = '<i class="icon-pencil"></i> Edit'.html_safe %>
<%= link_to edit_text, edit_admin_season_path(@season), :class => "btn" %>
<%= link_to 'Back', admin_seasons_path, :class => "btn" %>
