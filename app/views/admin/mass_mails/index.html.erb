<% search_fields = {
    subject_cont: { slug: 'defaults.subject' },
    body_cont:    { slug: 'defaults.description' }
} %>

<%= render 'shared/pages/index', resource_class: MassMail, resources: @mass_mails, content: render('index_results'), search_fields: search_fields %>
