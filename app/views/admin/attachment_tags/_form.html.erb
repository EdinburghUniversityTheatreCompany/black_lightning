<%= simple_horizontal_form_for [:admin, @attachment_tag] do |f| %>
  <%= render('shared/pages/form', f: f) do %>
    <% # BOOTSTRAP TODO This looks ugly. Largely down to the formatting of the fields tho. %>
    <%= f.input :name %>
    <%= render "/shared/form/md_editor", f: f, field: :description, rows: 12, show_hint: true %>
  <% end %>
<% end %>

