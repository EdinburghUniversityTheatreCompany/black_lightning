<%# locals: (paragraphs:) -%>

<% paragraphs.each do |paragraph| %>
  <p class="block mb-2 <%= paragraph[:class] %>">
    <%= '<small class="text-gray-500">'.html_safe if paragraph[:small] %>
    <%= paragraph[:content] %>
    <%= '</small>'.html_safe if paragraph[:small] %>
  </p>
<% end %>
