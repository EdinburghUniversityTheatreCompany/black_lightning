<% search_fields = {
    name_cont:        { slug: 'defaults.name' },
    tagline_cont:     { slug: 'defaults.tagline' },
    description_cont: { slug: 'defaults.description'},
    address_cont:     { slug: 'defaults.address'}
} %>

<% quick_actions = [get_link(Venue, :map, http_method: :get, link_text: generate_icon_prefix('map', 'Map'))] %>

<%= render 'shared/pages/index', resource_class: Venue, resources: @venues, content: render('index_results'), search_fields: search_fields, quick_actions: quick_actions %>
