<% search_fields = {
    user_full_name_cont: { slug: 'defaults.user_full_name' }
} %>

<%= render 'shared/pages/index', resource_class: MaintenanceCredit, resources: @maintenance_credits, content: render('index_results'), search_fields: search_fields %>
