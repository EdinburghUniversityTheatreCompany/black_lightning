<% fields = { 
  resolved: bool_text(@complaint.resolved),
  created_at: l(@complaint.created_at, format: :longy),
  description: { type: 'markdown', markdown: @complaint.description },
  comments: { type: 'markdown', markdown: @complaint.comments }
} %>

<%= render('shared/pages/show', object: @complaint, fields: fields) %>
