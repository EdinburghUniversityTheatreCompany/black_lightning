<% # BOOTSTRAP: Alex, can you check if this looks like you want it to look or if bits that are currently in the _table partial need to be moved to the generic index partial? %>
<% content = render('admin/marketing_creatives/categories/gallery', categories: @categories) %>

<% quick_actions = get_link(MarketingCreatives::Profile, :sign_up, http_method: :get) %>

<% search_fields = {
    name_cont: { slug: 'defaults.name' }
} %>

<%= render 'shared/pages/index', resource_class: MarketingCreatives::Category, resources: @categories, content: content, search_fields: search_fields, quick_actions: quick_actions %>
