<% search_fields = {
    name_cont:        { slug: 'defaults.name' },
    description_cont: { slug: 'defaults.description'}
} %>

<%= render 'shared/pages/index', resource_class: EventTag, resources: @event_tags, content: render('index_results'), search_fields: search_fields %>
