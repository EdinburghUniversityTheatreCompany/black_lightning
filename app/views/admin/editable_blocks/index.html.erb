<% search_fields = {
    name_cont:    { slug: 'defaults.name' },
    content_cont: { slug: 'defaults.content' },
    url_cont:     { slug: 'defaults.url' }
} %>

<% content = render('admin/editable_blocks/index_content') %>

<%= render 'shared/pages/index', resource_class: Admin::EditableBlock, content: content, search_fields: search_fields, 
  show_new_button: false, include_pagination: false %>

