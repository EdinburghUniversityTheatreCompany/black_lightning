<%= render '/shared/form/inline_fields_for_template',
    f: f,
    div_class_additions: 'team_member',
    fields: [
        f.hidden_field(:id) + 
        f.input_field(:position, collection: position_typeahead, allow_custom_input: true),
        f.association(:user, collection: User.all),
        link_to_remove(f)
] %>
