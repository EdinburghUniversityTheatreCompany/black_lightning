<% search_fields = {
    image_blob_filename_cont: { slug: 'defaults.filename' },
    gallery_of_Event_type_name_or_gallery_of_Venue_type_name_cont: { slug: 'defaults.gallery' },
    description_cont: { slug: 'defaults.description' },
    picture_tags_id_eq: { slug: 'defaults.picture_tags', type: :select, collection: PictureTag.accessible_by(current_ability) }
} %>

<% content = render('admin/pictures/index_content', pictures: @pictures) %>

<%= render 'shared/pages/index', resource_class: Picture, resources: @pictures, content: content, search_fields: search_fields, 
  show_new_button: false %>
