<%# locals: () %>

<%= simple_horizontal_form_for([:admin, @maintenance_credit]) do |f| %>
  <%= render('shared/pages/form', f: f) do %>
    <%= f.association :maintenance_session %>
    <%= f.association :user %>
  <% end %>
<% end %>
